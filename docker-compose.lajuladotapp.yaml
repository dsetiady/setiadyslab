services:
  app:
    image: localhost:3000/lajuladotapp/lajuladotapp:latest
    container_name: lajula.app
    restart: unless-stopped
    ports:
      - "8200:3000"
    networks:
      - shared-network

networks:
  shared-network:
  external: true